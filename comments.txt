# 1
    # Если какой-то пакет не устанавливается через пайчарм можно либо пипом либо посмотреть в документации
    # как его ставить
# 2
    # def get_movies(self) -> List[Movie]:  # Список экземпляров класса Movie; помогает ловить ошибки,
        # если вдруг не list вернул, а int или ещё чего похуже
    # ещё можно -> Optional[List[Movie], Movie] Либо один, либо несколько
# 3
    # Если на delete в сервисах и дао ничего не возвращать, сработает внутренний рейз ошибки из пакета sqlalchemy
# 4
    # location ещё вот так можно делать:

    # resp = make_response("", 201) """import from flask"""
    # resp.headers['location'] = f'{movie_ns.path}/{new_movie}'
    # return resp
# 5
    # вьюхи
    # сервисы
    # дао